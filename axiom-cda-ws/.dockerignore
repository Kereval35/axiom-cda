# Maven
target/classes
target/generated-sources
target/generated-test-sources
target/maven-status
target/test-classes
target/surefire-reports

# Keep only target/quarkus-app/ which is needed for Docker build

# Git
.git
.gitignore

# IDE
.idea
.vscode
*.iml
.classpath
.project
.settings

# Misc
*.log
.DS_Store

# Docs
docs/
README.md
prd.md

# Source (not needed since we copy from target/)
src/main/java
src/main/resources
src/test

# Maven wrapper (not needed in container)
.mvn
mvnw
mvnw.cmd

# Docker
Dockerfile*
docker-compose.yml
.dockerignore
